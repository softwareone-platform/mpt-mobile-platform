name: 'iOS Appium Testing'
description: 'Setup and run Appium tests on iOS simulator'
inputs:
  device_uuid:
    description: 'iOS simulator device UUID'
    required: true
  device_name:
    description: 'iOS simulator device name'
    required: false
    default: 'iPhone 16'
  ios_version:
    description: 'iOS version'
    required: false
    default: '26.0'
  node_version:
    description: 'Node.js version to use'
    required: false
    default: '20'
  appium_version:
    description: 'Appium version to install'
    required: false
    default: '3.1.1'
  app_bundle_id:
    description: 'iOS app bundle identifier'
    required: true
  appium_host:
    description: 'Appium server host'
    required: false
    default: 'localhost'
  appium_port:
    description: 'Appium server port'
    required: false
    default: '4723'
  airtable_email:
    description: 'Airtable email for OTP testing'
    required: true
  airtable_api_token:
    description: 'Airtable API token'
    required: true
  airtable_base_id:
    description: 'Airtable base ID'
    required: true
  airtable_table_name:
    description: 'Airtable table name'
    required: true
  airtable_from_email:
    description: 'Airtable from email filter'
    required: true
  # API Configuration (Optional - for API validation tests)
  api_base_url:
    description: 'API base URL for validation tests'
    required: false
    default: ''
  api_ops_token:
    description: 'API OPS token for validation tests'
    required: false
    default: ''
  # ReportPortal Configuration (Optional)
  report_portal_api_key:
    description: 'ReportPortal API key (leave empty to disable reporting)'
    required: false
    default: ''
  report_portal_endpoint:
    description: 'ReportPortal server endpoint'
    required: false
    default: ''
  report_portal_project:
    description: 'ReportPortal project name'
    required: false
    default: 'mpt-mobile'
  report_portal_launch_name:
    description: 'Name for the test launch in ReportPortal'
    required: false
    default: 'iOS Appium Tests'
  # Test Selection
  test_suite:
    description: 'Test suite to run (welcome, home, navigation, failing, all)'
    required: false
    default: 'welcome'
  test_spec:
    description: 'Specific test file to run (overrides test_suite if provided)'
    required: false
    default: ''

runs:
  using: 'composite'
  steps:
    - name: Verify Required Inputs
      shell: bash
      env:
        APP_BUNDLE_ID: ${{ inputs.app_bundle_id }}
        APPIUM_HOST: ${{ inputs.appium_host }}
        APPIUM_PORT: ${{ inputs.appium_port }}
        DEVICE_UUID: ${{ inputs.device_uuid }}
      run: |
        echo "🔍 Verifying required inputs..."
        
        # Check required inputs
        if [ -z "$APP_BUNDLE_ID" ]; then
          echo "❌ ERROR: app_bundle_id input is required"
          exit 1
        else
          echo "✅ app_bundle_id is set"
        fi
        
        # Check optional inputs (show defaults if not set)
        if [ -z "$APPIUM_HOST" ]; then
          echo "ℹ️  appium_host not set, using default: localhost"
        else
          echo "✅ appium_host is set"
        fi
        
        if [ -z "$APPIUM_PORT" ]; then
          echo "ℹ️  appium_port not set, using default: 4723"
        else
          echo "✅ appium_port is set"
        fi
        
        # Verify required inputs
        if [ -z "$DEVICE_UUID" ]; then
          echo "❌ ERROR: device_uuid input is required"
          exit 1
        else
          echo "✅ device_uuid input is provided: $DEVICE_UUID"
        fi
        
        echo "🎉 All required inputs are properly configured!"

    - name: Checkout test repository
      uses: actions/checkout@v4
      
    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: ${{ inputs.node_version }}
        cache: 'npm'
        cache-dependency-path: app/package-lock.json

    - name: Restore node modules cache
      id: npm-cache
      uses: actions/cache@v4
      with:
        path: |
          app/node_modules
          ~/.npm
        key: ${{ runner.os }}-node-${{ hashFiles('app/package-lock.json') }}
        restore-keys: |
          ${{ runner.os }}-node-

    - name: Install dependencies (only missing packages)
      shell: bash
      run: npm ci
      working-directory: ./app

    - name: Install Appium
      shell: bash
      env:
        APPIUM_VERSION: ${{ inputs.appium_version }}
      run: |
        npm install -g "appium@${APPIUM_VERSION}"
        appium --version
        
    - name: Install Appium XCUITest driver
      shell: bash
      run: |
        appium driver install xcuitest || echo "Driver already installed"
        appium driver list --installed
        
    - name: Pre-build WebDriverAgent for CI
      shell: bash
      env:
        DEVICE_UDID: ${{ inputs.device_uuid }}
      run: |
        echo "🔧 Pre-building WebDriverAgent to avoid CI connection issues..."
        
        # Get WebDriverAgent path
        WDA_PATH=$(find ~/.appium/node_modules -name "WebDriverAgent.xcodeproj" 2>/dev/null | head -1)
        
        if [ -n "$WDA_PATH" ]; then
          echo "📁 Found WebDriverAgent at: $WDA_PATH"
          WDA_DIR=$(dirname "$WDA_PATH")
          
          # Pre-build WebDriverAgent for the target simulator
          echo "🔨 Building WebDriverAgent for simulator..."
          cd "$WDA_DIR"
          
          xcodebuild build-for-testing \
            -project WebDriverAgent.xcodeproj \
            -scheme WebDriverAgentRunner \
            -destination "platform=iOS Simulator,id=$DEVICE_UDID" \
            -derivedDataPath /tmp/wda-derived-data \
            -quiet || echo "⚠️ WebDriverAgent pre-build failed, continuing anyway"
          
          echo "✅ WebDriverAgent pre-build completed"
        else
          echo "⚠️ WebDriverAgent not found, skipping pre-build"
        fi

    - name: Prepare simulator for XCUITest
      shell: bash
      env:
        DEVICE_UDID: ${{ inputs.device_uuid }}
        APP_BUNDLE_ID: ${{ inputs.app_bundle_id }}
      run: |
        echo "🔍 Preparing simulator for XCUITest in CI environment..."
        
        # Check simulator status
        SIMULATOR_STATUS=$(xcrun simctl list devices | grep "$DEVICE_UDID" | head -1 || echo "Not found")
        echo "Initial simulator status: $SIMULATOR_STATUS"
        
        if ! echo "$SIMULATOR_STATUS" | grep -q "(Booted)"; then
          echo "❌ ERROR: Simulator is not booted. Install workflow should have prepared this."
          echo "Expected simulator to be ready from previous workflow step."
          exit 1
        fi
        
        # CI-specific: Reset simulator state for clean XCUITest connection
        echo "🔄 Resetting simulator state for XCUITest..."
        xcrun simctl shutdown "$DEVICE_UDID" || true
        sleep 3
        
        echo "🚀 Booting simulator with fresh state..."
        xcrun simctl boot "$DEVICE_UDID"
        
        # Wait for simulator to be fully ready - use multiple readiness indicators
        echo "⏳ Waiting for simulator to be fully operational..."
        WAIT_COUNT=0
        MAX_WAIT=30
        BOOT_READY=false
        
        while [ $WAIT_COUNT -lt $MAX_WAIT ]; do
          # Check if simulator is booted (primary indicator)
          SIMULATOR_STATUS=$(xcrun simctl list devices | grep "$DEVICE_UDID" | head -1 || echo "Not found")
          if echo "$SIMULATOR_STATUS" | grep -q "(Booted)"; then
            echo "✅ Simulator is booted"
            
            # Try boot status check with timeout - if it hangs, skip it
            echo "🔍 Checking boot status readiness (with timeout)..."
            if timeout 5 xcrun simctl bootstatus "$DEVICE_UDID" 2>/dev/null | grep -q "Boot status is ready" 2>/dev/null; then
              echo "✅ Simulator boot status is ready"
              BOOT_READY=true
              break
            elif [ $? -eq 124 ]; then
              echo "⚠️ Boot status check timed out (5s), but simulator is booted - proceeding"
              BOOT_READY=true
              break
            else
              echo "📱 Simulator booted but boot status not ready yet, waiting..."
            fi
          else
            echo "⏳ Simulator not yet booted, waiting... (${WAIT_COUNT}/${MAX_WAIT})"
          fi
          
          sleep 2
          WAIT_COUNT=$((WAIT_COUNT + 1))
        done
        
        if [ "$BOOT_READY" = false ]; then
          if [ $WAIT_COUNT -eq $MAX_WAIT ]; then
            echo "⚠️ WARNING: Simulator readiness check timed out, but proceeding anyway"
          else
            echo "⚠️ WARNING: Could not verify simulator readiness, but proceeding anyway"
          fi
        fi
        
        # Additional delay for UI services to stabilize
        echo "⏳ Allowing UI services to stabilize..."
        sleep 5
        
        # CI-specific: Ensure Simulator UI is visible for WebDriverAgent
        echo "📺 Ensuring Simulator UI is visible for WebDriverAgent..."
        open -a Simulator.app --args -CurrentDeviceUDID "$DEVICE_UDID" || true
        sleep 3
        
        # Verify simulator UI is properly visible
        echo "🔍 Verifying Simulator UI state..."
        if pgrep -f "Simulator.app" > /dev/null; then
          echo "✅ Simulator UI is running"
        else
          echo "⚠️ WARNING: Simulator UI may not be visible"
        fi
        
        # Verify app is still installed after reset
        if xcrun simctl listapps "$DEVICE_UDID" 2>/dev/null | grep -q "$APP_BUNDLE_ID"; then
          echo "✅ App is installed and ready"
        else
          echo "⚠️ WARNING: Could not verify app installation, but proceeding with test"
          echo "Appium will provide better error messages if app is missing"
        fi
        
        echo "🎯 Simulator is prepared for XCUITest"
        
    - name: Start Appium Server
      shell: bash
      env:
        APPIUM_HOST: ${{ inputs.appium_host }}
        APPIUM_PORT: ${{ inputs.appium_port }}
      run: |
        echo "Starting Appium server..."
        appium --log-level info --log appium.log &
        APPIUM_PID=$!
        echo ${APPIUM_PID} > appium.pid
        echo "Appium PID: ${APPIUM_PID}"
        
        # Wait for Appium to start
        APPIUM_WAIT_SECONDS=65
        echo "Waiting for Appium server to start (timeout: ${APPIUM_WAIT_SECONDS}s)..."
        for i in $(seq 1 ${APPIUM_WAIT_SECONDS}); do
          if curl -s "http://${APPIUM_HOST}:${APPIUM_PORT}/status" > /dev/null; then
            echo "✓ Appium server is ready"
            break
          fi
          if [ $i -eq ${APPIUM_WAIT_SECONDS} ]; then
            echo "✗ Appium server failed to start within ${APPIUM_WAIT_SECONDS} seconds"
            cat appium.log
            exit 1
          fi
          echo "Waiting... ($i/${APPIUM_WAIT_SECONDS})"
          sleep 1
        done
        
        # Extract working URLs from Appium logs for WebDriver connection
        echo "🔍 Extracting Appium connection URLs from logs..."
        NETWORK_URL=$(grep -E "http://[0-9]+\.[0-9]+\.[0-9]+\.[0-9]+:${APPIUM_PORT}/" appium.log | head -1 | sed 's/.*\(http:\/\/[0-9\.]*:[0-9]*\)\/.*/\1/')
        LOCALHOST_URL="http://127.0.0.1:${APPIUM_PORT}"
        
        if [ -n "$NETWORK_URL" ]; then
          echo "✅ Found network URL: $NETWORK_URL"
          echo "VERIFIED_APPIUM_URL=$NETWORK_URL" >> $GITHUB_ENV
        else
          echo "⚠️ Network URL not found, using localhost"
          echo "VERIFIED_APPIUM_URL=$LOCALHOST_URL" >> $GITHUB_ENV
        fi
        
        echo "📋 Available Appium URLs for WebDriver:"
        echo "  - Localhost: $LOCALHOST_URL"
        [ -n "$NETWORK_URL" ] && echo "  - Network: $NETWORK_URL"
        
    - name: Verify Appium Server
      shell: bash
      env:
        APPIUM_HOST: ${{ inputs.appium_host }}
        APPIUM_PORT: ${{ inputs.appium_port }}
      run: |
        echo "Verifying Appium server status..."
        curl -f "http://${APPIUM_HOST}:${APPIUM_PORT}/status" | jq '.'

    - name: Pre-test Debug Information
      shell: bash
      env:
        DEVICE_UDID: ${{ inputs.device_uuid }}
        DEVICE_NAME: ${{ inputs.device_name }}
        PLATFORM_VERSION: ${{ inputs.ios_version }}
        APP_BUNDLE_ID: ${{ inputs.app_bundle_id }}
        APPIUM_HOST: ${{ inputs.appium_host }}
        APPIUM_PORT: ${{ inputs.appium_port }}
      run: |
        echo "=== Pre-test Debug Information ==="
        echo "Environment Variables:"
        echo "DEVICE_UDID: $DEVICE_UDID"
        echo "DEVICE_NAME: $DEVICE_NAME"
        echo "PLATFORM_VERSION: $PLATFORM_VERSION"
        echo "APP_BUNDLE_ID: $APP_BUNDLE_ID"
        echo "APPIUM_HOST: $APPIUM_HOST"
        echo "APPIUM_PORT: $APPIUM_PORT"
        echo "VERIFIED_APPIUM_URL: ${VERIFIED_APPIUM_URL:-'Not set'}"
        
        echo "Current Simulator Status:"
        xcrun simctl list devices | grep "$DEVICE_UDID" || echo "Device not found"
        
        echo "App Installation Status:"
        if xcrun simctl list devices | grep "$DEVICE_UDID" | grep -q "(Booted)"; then
          xcrun simctl listapps "$DEVICE_UDID" | grep -i "$APP_BUNDLE_ID" || echo "App not found"
        else
          echo "Cannot check app status - simulator not booted"
        fi
        
        echo "Appium Server Status Check:"
        echo "  Testing default URL: http://${APPIUM_HOST}:${APPIUM_PORT}/status"
        curl -s "http://${APPIUM_HOST}:${APPIUM_PORT}/status" | jq '.' || echo "Default URL not reachable"
        
        if [ -n "$VERIFIED_APPIUM_URL" ]; then
          echo "  Testing verified URL: ${VERIFIED_APPIUM_URL}/status"
          curl -s "${VERIFIED_APPIUM_URL}/status" | jq '.' || echo "Verified URL not reachable"
        fi
        
    - name: Run Appium Tests
      shell: bash
      working-directory: ./app
      continue-on-error: false
      env:
        DEVICE_UDID: ${{ inputs.device_uuid }}
        DEVICE_NAME: ${{ inputs.device_name }}
        PLATFORM_VERSION: ${{ inputs.ios_version }}
        APP_BUNDLE_ID: ${{ inputs.app_bundle_id }}
        AIRTABLE_EMAIL: ${{ inputs.airtable_email }}
        AIRTABLE_API_TOKEN: ${{ inputs.airtable_api_token }}
        AIRTABLE_BASE_ID: ${{ inputs.airtable_base_id }}
        AIRTABLE_TABLE_NAME: ${{ inputs.airtable_table_name }}
        AIRTABLE_FROM_EMAIL: ${{ inputs.airtable_from_email }}
        # API configuration (optional - for API validation tests)
        API_BASE_URL: ${{ inputs.api_base_url }}
        API_OPS_TOKEN: ${{ inputs.api_ops_token }}
        # ReportPortal configuration (optional)
        REPORT_PORTAL_API_KEY: ${{ inputs.report_portal_api_key }}
        REPORT_PORTAL_ENDPOINT: ${{ inputs.report_portal_endpoint }}
        REPORT_PORTAL_PROJECT: ${{ inputs.report_portal_project }}
        REPORT_PORTAL_LAUNCH_NAME: ${{ inputs.report_portal_launch_name }}
        REPORT_PORTAL_LAUNCH_DESCRIPTION: 'iOS ${{ inputs.device_name }} (${{ inputs.ios_version }}) - GitHub Actions Run #${{ github.run_number }}'
      run: |
        echo "🚀 Starting WebDriver tests with verified Appium connection..."
        
        # Priority: Use verified network URL if available, otherwise use input values
        if [ -n "$VERIFIED_APPIUM_URL" ]; then
          EXTRACTED_HOST=$(echo "$VERIFIED_APPIUM_URL" | sed 's|http://\([^:]*\):.*|\1|')
          EXTRACTED_PORT=$(echo "$VERIFIED_APPIUM_URL" | sed 's|.*:\([0-9]*\)|\1|')
          
          echo "📡 Using verified network connection: $VERIFIED_APPIUM_URL"
          echo "   Host: $EXTRACTED_HOST"
          echo "   Port: $EXTRACTED_PORT"
          
          export APPIUM_HOST="$EXTRACTED_HOST"
          export APPIUM_PORT="$EXTRACTED_PORT"
        else
          # Fall back to input values
          export APPIUM_HOST="${{ inputs.appium_host }}"
          export APPIUM_PORT="${{ inputs.appium_port }}"
          echo "⚠️  Using input values: http://$APPIUM_HOST:$APPIUM_PORT"
        fi
        
        # Test the connection before running tests
        echo "🔧 Testing WebDriver connection..."
        WEBDRIVER_URL="http://${APPIUM_HOST}:${APPIUM_PORT}"
        if curl -s "$WEBDRIVER_URL/status" > /dev/null; then
          echo "✅ WebDriver connection test successful: $WEBDRIVER_URL"
        else
          echo "❌ WebDriver connection test failed: $WEBDRIVER_URL"
          echo "Trying localhost fallback..."
          export APPIUM_HOST="localhost"
          if curl -s "http://localhost:${APPIUM_PORT}/status" > /dev/null; then
            echo "✅ Localhost fallback successful"
          else
            echo "❌ All connection attempts failed"
            exit 1
          fi
        fi
        
        echo "🎯 Running tests with APPIUM_HOST=$APPIUM_HOST APPIUM_PORT=$APPIUM_PORT"
        echo "📋 WebDriverIO Environment Variables:"
        echo "   DEVICE_UDID: $DEVICE_UDID"
        echo "   DEVICE_NAME: $DEVICE_NAME"
        echo "   PLATFORM_VERSION: $PLATFORM_VERSION"
        echo "   APP_BUNDLE_ID: $APP_BUNDLE_ID"
        echo "   APPIUM_HOST: $APPIUM_HOST"
        echo "   APPIUM_PORT: $APPIUM_PORT"
        echo ""
        echo "📧 Airtable Configuration:"
        echo "   AIRTABLE_EMAIL: ${AIRTABLE_EMAIL}"
        echo "   AIRTABLE_API_TOKEN: ${AIRTABLE_API_TOKEN:+<configured (${#AIRTABLE_API_TOKEN} chars)>}"
        echo "   AIRTABLE_BASE_ID: ${AIRTABLE_BASE_ID}"
        echo "   AIRTABLE_TABLE_NAME: ${AIRTABLE_TABLE_NAME}"
        echo "   AIRTABLE_FROM_EMAIL: ${AIRTABLE_FROM_EMAIL}"
        
        # Determine test command based on inputs
        TEST_SUITE="${{ inputs.test_suite }}"
        TEST_SPEC="${{ inputs.test_spec }}"
        
        echo ""
        echo "🚀 Starting WDIO tests..."
        if [ -n "$TEST_SPEC" ]; then
          # Check if TEST_SPEC looks like a suite name (no path separators or .js extension)
          # Suite names are simple identifiers like: welcome, featureFlags, navigation
          # NOTE: This heuristic assumes suite names don't contain dots. If suite naming
          # conventions change (e.g., my.suite), update this logic AND the Android action.
          if [[ ! "$TEST_SPEC" =~ [/\.] ]]; then
            echo "📋 Running test suite (detected from spec): $TEST_SPEC"
            npx wdio run wdio.conf.js --suite "$TEST_SPEC"
          else
            echo "📋 Running specific test file: $TEST_SPEC"
            npx wdio run wdio.conf.js --spec "$TEST_SPEC"
          fi
        elif [ "$TEST_SUITE" = "all" ]; then
          echo "📋 Running all test suites"
          npx wdio run wdio.conf.js
        else
          echo "📋 Running test suite: $TEST_SUITE"
          npx wdio run wdio.conf.js --suite "$TEST_SUITE"
        fi
        
    - name: Upload Appium logs
      if: always()
      uses: actions/upload-artifact@v4
      with:
        name: appium-logs-${{ github.run_number }}
        path: appium.log
        retention-days: 7
        if-no-files-found: warn
        
    - name: Upload test results
      if: always()
      uses: actions/upload-artifact@v4
      with:
        name: test-results-${{ github.run_number }}
        path: |
          app/test-results/
          screenshots/
        retention-days: 7
        if-no-files-found: ignore
        
    - name: Capture simulator logs
      if: always()
      shell: bash
      env:
        DEVICE_UDID: ${{ inputs.device_uuid }}
      run: |
        echo "Capturing simulator logs..."
        xcrun simctl spawn "$DEVICE_UDID" log collect --output simulator.log || true
        
    - name: Upload simulator logs
      if: always()
      uses: actions/upload-artifact@v4
      with:
        name: simulator-logs-${{ github.run_number }}
        path: simulator.log
        retention-days: 7
        if-no-files-found: ignore
        
    - name: Shutdown Simulator
      if: always()
      shell: bash
      env:
        DEVICE_UDID: ${{ inputs.device_uuid }}
      run: |
        echo "Shutting down simulator..."
        xcrun simctl shutdown "$DEVICE_UDID" || true
        echo "✓ Simulator shutdown complete"
        
    - name: Stop Appium Server
      if: always()
      shell: bash
      run: |
        if [ -f appium.pid ]; then
          APPIUM_PID=$(cat appium.pid)
          echo "Stopping Appium server (PID: ${APPIUM_PID})..."
          kill ${APPIUM_PID} || true
          rm appium.pid
          echo "✓ Appium server stopped"
        fi
        
    - name: Cleanup
      if: always()
      shell: bash
      env:
        DEVICE_UDID: ${{ inputs.device_uuid }}
        APP_BUNDLE_ID: ${{ inputs.app_bundle_id }}
      run: |
        echo "Cleaning up test artifacts..."
        # Optional: Remove app from simulator
        # xcrun simctl uninstall "$DEVICE_UDID" "$APP_BUNDLE_ID" || true
        echo "✓ Cleanup complete"