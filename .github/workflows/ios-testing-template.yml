name: iOS Appium Tests

permissions:
  contents: read

on:
  workflow_call:
    inputs:
      device_uuid:
        description: 'iOS simulator device UUID'
        required: true
        type: string
      ios_version:
        description: 'iOS version for simulator'
        required: false
        type: string
        default: '26.0'
      device_name:
        description: 'iOS simulator device name'
        required: false
        type: string
        default: 'iPhone 16'
      node_version:
        description: 'Node.js version to use'
        required: false
        type: string
        default: '20'
      appium_version:
        description: 'Appium version to install'
        required: false
        type: string
        default: '3.1.1'

    secrets:
      APP_BUNDLE_ID:
        description: 'iOS app bundle identifier'
        required: true
      APPIUM_HOST:
        description: 'Appium server host'
        required: false
      APPIUM_PORT:
        description: 'Appium server port'
        required: false

jobs:
  ios-simulator-tests:
    runs-on: macos-latest
    timeout-minutes: 60
    
    env:
      AUTOMATION_NAME: XCUITest
      APPIUM_HOST: ${{ secrets.APPIUM_HOST || 'http://localhost' }}
      APPIUM_PORT: ${{ secrets.APPIUM_PORT || '4723' }}
      APP_BUNDLE_ID: ${{ secrets.APP_BUNDLE_ID }}
      DEVICE_UDID: ${{ inputs.device_uuid }}
      DEVICE_NAME: ${{ inputs.device_name }}
      PLATFORM_VERSION: ${{ inputs.ios_version }}
      PLATFORM_NAME: iOS
      
    steps:
      - name: Verify Required Secrets
        run: |
          echo "🔍 Verifying required secrets and environment variables..."
          
          # Check required secrets
          if [ -z "${{ secrets.APP_BUNDLE_ID }}" ]; then
            echo "❌ ERROR: APP_BUNDLE_ID secret is not set"
            echo "Please set this secret in GitHub: Settings → Secrets and variables → Actions → Secrets"
            exit 1
          else
            echo "✅ APP_BUNDLE_ID is set"
          fi
          
          # Check optional secrets (show defaults if not set)
          if [ -z "${{ secrets.APPIUM_HOST }}" ]; then
            echo "ℹ️  APPIUM_HOST not set, using default: http://localhost"
          else
            echo "✅ APPIUM_HOST is set"
          fi
          
          if [ -z "${{ secrets.APPIUM_PORT }}" ]; then
            echo "ℹ️  APPIUM_PORT not set, using default: 4723"
          else
            echo "✅ APPIUM_PORT is set"
          fi
          
          # Verify required inputs
          if [ -z "${{ inputs.device_uuid }}" ]; then
            echo "❌ ERROR: device_uuid input is required"
            exit 1
          else
            echo "✅ device_uuid input is provided: ${{ inputs.device_uuid }}"
          fi
          
          echo "🎉 All required secrets and inputs are properly configured!"

      - name: Checkout test repository
        uses: actions/checkout@v4
        
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ inputs.node_version }}
          cache: 'npm'
          cache-dependency-path: app/package-lock.json

      - name: Restore node modules cache
        id: npm-cache
        uses: actions/cache@v4
        with:
          path: |
            app/node_modules
            ~/.npm
          key: ${{ runner.os }}-node-${{ hashFiles('app/package-lock.json') }}
          restore-keys: |
            ${{ runner.os }}-node-

      - name: Install dependencies (only missing packages)
        run: npm ci
        working-directory: ./app

      - name: Install Appium
        run: |
          npm install -g appium@${{ inputs.appium_version }}
          appium --version
          
      - name: Install Appium XCUITest driver
        run: |
          appium driver install xcuitest || echo "Driver already installed"
          appium driver list --installed
          
      - name: Start Appium Server
        run: |
          echo "Starting Appium server..."
          appium --log-level info --log appium.log &
          APPIUM_PID=$!
          echo ${APPIUM_PID} > appium.pid
          echo "Appium PID: ${APPIUM_PID}"
          
          # Wait for Appium to start
          APPIUM_WAIT_SECONDS=65
          echo "Waiting for Appium server to start (timeout: ${APPIUM_WAIT_SECONDS}s)..."
          for i in $(seq 1 ${APPIUM_WAIT_SECONDS}); do
            if curl -s "${APPIUM_HOST}:${APPIUM_PORT}/status" > /dev/null; then
              echo "✓ Appium server is ready"
              break
            fi
            if [ $i -eq ${APPIUM_WAIT_SECONDS} ]; then
              echo "✗ Appium server failed to start within ${APPIUM_WAIT_SECONDS} seconds"
              cat appium.log
              exit 1
            fi
            echo "Waiting... ($i/${APPIUM_WAIT_SECONDS})"
            sleep 1
          done
          
      - name: Verify Appium Server
        run: |
          echo "Verifying Appium server status..."
          curl -f "${APPIUM_HOST}:${APPIUM_PORT}/status" | jq '.'

      # Running the Appium tests:
      - name: Run Appium Tests
        run: npx wdio run wdio.conf.js --suite welcome
        working-directory: ./app
        continue-on-error: false
          
      - name: Upload Appium logs
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: appium-logs-${{ github.run_number }}
          path: appium.log
          retention-days: 7
          if-no-files-found: warn
          
      - name: Upload test results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: test-results-${{ github.run_number }}
          path: |
            test-results/
            screenshots/
          retention-days: 7
          if-no-files-found: ignore
          
      - name: Capture simulator logs
        if: always()
        run: |
          echo "Capturing simulator logs..."
          xcrun simctl spawn ${{ env.DEVICE_UDID }} log collect --output simulator.log || true
          
      - name: Upload simulator logs
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: simulator-logs-${{ github.run_number }}
          path: simulator.log
          retention-days: 7
          if-no-files-found: ignore
          
      - name: Shutdown Simulator
        if: always()
        run: |
          echo "Shutting down simulator..."
          xcrun simctl shutdown ${{ env.DEVICE_UDID }} || true
          echo "✓ Simulator shutdown complete"
          
      - name: Stop Appium Server
        if: always()
        run: |
          if [ -f appium.pid ]; then
            APPIUM_PID=$(cat appium.pid)
            echo "Stopping Appium server (PID: ${APPIUM_PID})..."
            kill ${APPIUM_PID} || true
            rm appium.pid
            echo "✓ Appium server stopped"
          fi
          
      - name: Cleanup
        if: always()
        run: |
          echo "Cleaning up test artifacts..."
          # Optional: Remove app from simulator
          # xcrun simctl uninstall ${{ env.DEVICE_UDID }} ${{ env.APP_BUNDLE_ID }} || true
          echo "✓ Cleanup complete"
